# Ejemplo de uso de CodeCarbon

Instalar codecarbon y tensorflow. El resto es por si quieres ver un visualizador en dash

- pip install codecarbon
- pip install tensorflow
- pip install dash
- pip install dbc
- pip install dash-bootstrap-components
- pip install fire
